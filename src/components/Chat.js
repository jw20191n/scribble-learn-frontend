import React from 'react';
import io from 'socket.io-client';

let socket;
export default function Chat(props) {
    // if(!socket){
    //     socket = io(':3002')
    // }

    return (
        <div>
            chats goes in here       
        </div>
    )
    
}